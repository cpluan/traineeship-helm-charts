apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: aws-load-balancer-serving-cert
  namespace: kube-system
spec:
  dnsNames:
    - aws-load-balancer-webhook.kube-system.svc
  secretName: aws-load-balancer-webhook-tls
  issuerRef:
    name: aws-lb-selfsigned-issuer
    kind: Issuer
