apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: httpbin-test-application
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7a"
spec:
  project: default
  source:
    repoURL: "https://github.com/cpluan/traineeship-helm-charts.git"
    targetRevision: benchmark/ingress-v1
    path: benchmark/http-load/
  destination:
    server: https://kubernetes.default.svc
    namespace: benchmark
  syncPolicy:
    automated:
      selfHeal: true
