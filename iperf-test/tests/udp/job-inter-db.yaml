apiVersion: batch/v1
kind: Job
metadata:
  name: iperf-client-inter-db-udp
spec:
  template:
    metadata:
      labels:
        app: iperf-client-inter-db-udp
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: iperf-server
              topologyKey: "kubernetes.io/hostname"
      restartPolicy: Never
      containers:
        - name: iperf-client
          image: amazonlinux:2023
          command: ["/bin/bash", "-c"]
          args:
            - |
              echo "Aguardando 70 minutos antes de iniciar o iperf..." && \
              sleep 12 && \
              dnf update &&\
              dnf install awscli -y && \
              yum install -y iperf3 jq unzip && \
              iperf3 -c iperf-server --udp -b 500M -J > /tmp/result.json && \
              aws s3 cp /tmp/result.json s3://ec2-rke2-benchmark-data/iperf/inter/result-$(date +%s).json && \
              echo "[OK] JSON enviado para o S3"
