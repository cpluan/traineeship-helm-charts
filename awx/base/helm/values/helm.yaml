apiVersion: builtin
kind: HelmChartInflationGenerator
metadata:
  name: awx-operator
name: awx-operator
chartName: awx-operator
repo: https://ansible.github.io/awx-operator/
version: 2.19.1
releaseName: awx-operator
namespace: awx
valuesInline:

  # Adicional: Defina se deseja CRDs instalados via Helm
  crds:
    install: true